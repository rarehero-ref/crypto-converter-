name: crypto_converter
description: A crypto & fiat currency converter using CoinGecko API.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  assets:
    - assets/logo.png
